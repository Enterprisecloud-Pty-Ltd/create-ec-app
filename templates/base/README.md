# EC React application

Run `npm install`, then `npm run dev` to start the app.

Run `npm run check` for TypeScript and lint checks, and `npm run build` for a production build. See [docs/tooling.md](docs/tooling.md) for editor setup and dependency maintenance.
