{
  "name": "{{PCF_PACKAGE_NAME}}",
  "version": "{{PCF_VERSION}}",
  "private": true,
  "description": "{{CONTROL_DESCRIPTION}}",
  "scripts": {
    "build": "node scripts/run-pcf.mjs build",
    "clean": "pcf-scripts clean",
    "lint": "node scripts/run-pcf.mjs lint",
    "rebuild": "npm run clean && npm run build",
    "start": "pcf-scripts start",
    "push": "pcf-scripts push"
  },
  "devDependencies": {
    "@types/powerapps-component-framework": "^1.3.18",
    "pcf-start": "^1.51.1",
    "pcf-scripts": "^1.51.1",
    "typescript": "^5.9.3"
  }
}
